# ShadowMe 产品体验审查报告

## 🔴 P0 - 阻断性问题（不修复就无法使用）

### 1. Task Detail 页面使用硬编码 demo 数据 → 无法查看真实任务详情
- **问题描述**：`tasks/[id]/page.tsx` 第 87 行 `const task = demoTask;`，页面直接使用硬编码的 `demoTask` 数据，完全忽略 `params.id`
- **影响**：用户点击任何任务卡片进入详情页，看到的都是同一个示例任务，无法查看和操作真实任务
- **建议修复方案**：
```typescript
// 应该从 API 获取真实数据
const task = state.tasks.find(t => t.id === params.id) || demoTask;
```

### 2. 删除任务没有任何确认机制
- **问题描述**：`tasks/page.tsx` 第 227 行直接调用 `deleteTask(task.id)`，没有任何二次确认
- **影响**：误点击删除按钮会直接删除任务，无挽回余地
- **建议修复方案**：使用 `ConfirmDialog` 组件或原生 `window.confirm()` 确认

### 3. 创建任务成功后没有任何成功反馈
- **问题描述**：`tasks/new/page.tsx` 的 `handleSubmit` 在 `createTask` 成功后只重置了表单，没有跳转到看板或显示成功提示
- **影响**：用户不知道任务是否真的创建成功
- **建议修复方案**：添加 toast 提示 + 自动跳转到 Dashboard

### 4. 移动端表单输入体验差
- **问题描述**：`tasks/new/page.tsx` 的 `Task Title` 和 `Problem Description` 输入框在移动端没有针对文本输入优化
- **影响**：移动端输入时键盘弹出遮挡表单，影响填写体验
- **建议修复方案**：`viewport` 添加 `viewport-fit=cover`，使用 `scrollIntoView()` 聚焦时调整滚动

---

## 🟠 P1 - 严重体验问题

### 5. 缺失面包屑导航
- **问题描述**：Task Detail、Archive 等深层页面没有面包屑，用户迷路后不知如何返回
- **影响**：用户操作后难以回到上级页面
- **建议修复方案**：在页面顶部添加面包屑导航，如：Dashboard > Task List > TASK-8492

### 6. 附件上传功能是"空壳"
- **问题描述**：`tasks/new/page.tsx` 第 190-203 行的附件上传区域只有 UI，没有实际的拖拽上传逻辑
- **影响**：用户看到可上传的提示，但无法实际上传文件
- **建议修复方案**：实现文件拖拽/选择逻辑，或至少禁用/隐藏此区域

### 7. 设置页面"保存"按钮无实际效果
- **问题描述**：`settings/page.tsx` 的 `handleSave` 只是模拟 1 秒延迟，没有真正的持久化逻辑
- **影响**：用户修改配置后刷新页面，设置全部丢失
- **建议修复方案**：使用 `localStorage` 或 API 持久化配置

### 8. 任务卡片点击区域不明确
- **问题描述**：`page.tsx` 的 `TaskCardComponent` 只有点击标题才能跳转，点击卡片其他区域没有反应
- **影响**：用户可能尝试点击整个卡片进入详情，但实际只有标题可点击
- **建议修复方案**：将整个卡片包裹在 `<Link>` 或添加 `onClick` 跳转

### 9. 缺失 WebSocket 实时连接反馈
- **问题描述**：`AppContext` 中有 `wsConnected` 状态，但 UI 上只有 `Navbar` 有 Wifi 图标显示，Dashboard 上虽然有 Shadow.OS 状态卡片但不知道是否真的连接
- **影响**：用户无法确认分身是否真的在运行
- **建议修复方案**：在 Shadow.OS 状态卡片中添加更明确的连接状态指示

### 10. 空状态处理不够友好
- **问题描述**：`page.tsx` 第 199-203 行空状态只显示 "No tasks"，没有引导用户创建任务
- **影响**：首次访问用户可能困惑该做什么
- **建议修复方案**：添加"创建第一个任务"按钮引导

---

## 🟡 P2 - 体验优化建议

### 11. Create Task 表单缺少任务类型选择
- **问题描述**：`tasks/new/page.tsx` 有 `type` 字段（`technical_issue` 默认值）但表单中没有让用户选择
- **影响**：用户无法指定任务类型，所有任务都是同一类型
- **建议修复方案**：参考 `TaskForm.tsx` 添加任务类型选择器

### 12. 优先级按钮缺少"紧急"选项
- **问题描述**：`tasks/new/page.tsx` 第 30-34 行只有 `low/medium/high` 三个优先级，但类型定义有 `urgent`
- **影响**：需要标记紧急任务时无法实现
- **建议修复方案**：添加"紧急"优先级按钮

### 13. 移动端底部导航栏遮挡内容
- **问题描述**：`Sidebar.tsx` 的移动端底部导航固定高度 64px，但页面内容没有底部留白
- **影响**：页面最底部的内容被导航栏遮挡
- **建议修复方案**：在移动端内容容器添加 `pb-20` 底部内边距

### 14. 任务描述超长时没有截断或展开
- **问题描述**：`tasks/page.tsx` 使用 `line-clamp-2` 截断，但 Task Detail 页面长描述会撑破布局
- **影响**：任务描述过长时影响页面美观
- **建议修复方案**：统一使用可展开的描述组件

### 15. Archive 页面硬编码数据不可用
- **问题描述**：`archive/page.tsx` 所有归档数据都是硬编码的 `archivedTasks` 数组
- **影响**：无法看到真实的归档历史
- **建议修复方案**：接入 API 获取真实归档数据

### 16. Settings 的 Local Endpoint 是只读字段
- **问题描述**：`settings/page.tsx` 第 186 行 `readOnly`，用户无法修改
- **影响**：用户想要连接不同的本地端点时无法配置
- **建议修复方案**：移除只读或添加编辑功能

### 17. 缺少任务完成后的 Merge Request 链接展示
- **问题描述**：`Task Detail` 页面有"Create Merge Request"按钮但没有展示已创建的 MR 链接
- **影响**：任务完成后用户不知道结果在哪里
- **建议修复方案**：完成后显示 GitLab MR 链接卡片

### 18. 移动端侧边栏关闭按钮位置不便
- **问题描述**：`Sidebar.tsx` 第 213 行关闭按钮在右上角，与汉堡菜单位置不对应
- **影响**：用户习惯从左侧滑入的菜单从左侧关闭
- **建议修复方案**：将关闭按钮移到左上角

---

## 📊 页面逐个审查

### Dashboard (page.tsx)

| 维度 | 状态 | 问题 |
|------|------|------|
| 首次访问体验 | ⚠️ | 没有欢迎引导，直接显示看板 |
| 空状态处理 | ⚠️ | 显示"No tasks"但没有引导创建 |
| 加载状态 | ✅ | 有 `isLoading` 状态和 Demo 数据兜底 |
| 错误状态 | ❌ | API 失败时没有错误提示 |
| 边界条件 | ⚠️ | 任务标题超长没有截断 |
| 操作反馈 | ⚠️ | 卡片没有明显的可点击提示 |

**亮点**：
- Shadow.OS 状态 widget 已实现（设计稿要求 ✓）
- 处理中卡片的迷你终端日志已实现（设计稿要求 ✓）
- 三列看板布局符合设计

**问题**：
- 没有"待领取"列的领取按钮
- 分身处理中列应该有"取消"或"查看详情"按钮

---

### Create Task (tasks/new/page.tsx)

| 维度 | 状态 | 问题 |
|------|------|------|
| 表单验证 | ⚠️ | 标题有 `required`，但没有实时校验 |
| 字段完整性 | ⚠️ | 缺少任务类型选择 |
| 附件功能 | ❌ | 只有 UI，没有实际功能 |
| 提交反馈 | ❌ | 没有成功/失败提示 |
| 取消操作 | ✅ | Cancel 按钮可返回 Dashboard |

**设计稿差距**：
- 右侧系统状态面板功能完整 ✓
- 但 Current Workload 列表是硬编码数据

---

### Task Detail (tasks/[id]/page.tsx)

| 维度 | 状态 | 问题 |
|------|------|------|
| 数据真实性 | 🔴 | 使用 demoTask 硬编码数据 |
| 返回导航 | ⚠️ | 只有"Back to Dashboard"文字链接 |
| CC Sync Log | ✅ | 实现完整，符合设计稿 |
| 实时通信 | ⚠️ | 模拟数据，消息发送是假的 |
| GitLab 集成 | ⚠️ | 有 UI 按钮，没有实际功能 |

**严重问题**：
- `params.id` 完全被忽略，任何任务 ID 都显示同一个任务

---

### Task List (tasks/page.tsx)

| 维度 | 状态 | 问题 |
|------|------|------|
| 搜索功能 | ✅ | 支持标题搜索 |
| 多维筛选 | ✅ | 支持状态/优先级/类型筛选 |
| 空状态 | ✅ | 根据筛选条件给出不同提示 |
| 列表操作 | ⚠️ | 编辑/删除没有确认 |

**问题**：
- 删除操作没有确认
- 没有批量操作功能

---

### Archive (archive/page.tsx)

| 维度 | 状态 | 问题 |
|------|------|------|
| 数据真实性 | ❌ | 所有数据都是硬编码 |
| 搜索功能 | ✅ | 支持搜索 |
| 布局设计 | ✅ | Bento Grid 布局美观 |
| 空状态 | ⚠️ | 搜索无结果是只显示图标 |

**问题**：
- 无法查看真实归档任务
- 统计数据 `138+` 是假的

---

### Settings (settings/page.tsx)

| 维度 | 状态 | 问题 |
|------|------|------|
| 配置项 | ✅ | 覆盖 GitLab、本地端点、身份配置 |
| 表单保存 | ❌ | 保存后刷新丢失 |
| 连接测试 | ❌ | "Test Connection"没有实际功能 |
| 主题切换 | ✅ | 正常工作 |
| 字段只读 | ⚠️ | Local Endpoint 是只读 |

**问题**：
- 所有配置修改后无法持久化
- Connection Status 是手动切换的假状态

---

### 公共组件

#### Sidebar
- 移动端实现完整：顶部 App Bar + 侧滑抽屉 + 底部 Tab Bar ✓
- 移动端关闭按钮位置不够直观
- 底部"Support"和"Documentation"链接是死链

#### Navbar（与 Sidebar 功能重复）
- 代码中有 `Navbar.tsx`，但 `AppLayout.tsx` 可能使用的是 `Sidebar`
- 存在冗余，建议统一

#### AppContext
- WebSocket 连接状态已定义但未实际使用
- 错误处理不够完善（只有 console.error）

---

## 🎯 优先修复排序（Top 10）

| 优先级 | 问题 | 预估工作量 |
|--------|------|-----------|
| 1 | Task Detail 使用 demo 数据而非真实数据 | 10 分钟 |
| 2 | 创建任务后无反馈/跳转 | 15 分钟 |
| 3 | 删除任务无确认 | 5 分钟 |
| 4 | 附件上传功能缺失或隐藏 | 2 小时 |
| 5 | 设置保存不持久化 | 30 分钟 |
| 6 | 添加面包屑导航 | 30 分钟 |
| 7 | 卡片点击区域扩大到整个卡片 | 15 分钟 |
| 8 | API 错误状态处理 | 20 分钟 |
| 9 | 移动端内容被底部导航遮挡 | 5 分钟 |
| 10 | 设置页面"Test Connection"功能 | 1 小时 |

---

## 与设计稿差距总结

| 设计稿要求 | 实现状态 |
|-----------|---------|
| Dashboard 的 Shadow.OS 状态 widget | ✅ 已实现 |
| 处理中卡片的迷你终端日志 | ✅ 已实现 |
| Create Task 右侧系统状态面板 | ✅ 已实现，功能完整 |
| CC Sync Log 实时消息流 | ✅ 已实现 |
| Bento Grid 归档布局 | ✅ 已实现 |
| 多主题切换 | ⚠️ 功能正常但部分样式不完整 |

---

## 总结

ShadowMe 的 UI 设计非常出色，多主题系统完善，动画流畅。但核心功能存在**严重缺陷**：

1. **数据层几乎是空的**：所有页面都在使用硬编码的 Demo 数据
2. **表单提交没有反馈**：用户不知道操作是否成功
3. **关键操作无确认**：删除等不可逆操作没有保护
4. **移动端体验需要优化**：底部导航遮挡、侧边栏关闭位置

建议优先修复 P0 问题后再进行体验优化。
